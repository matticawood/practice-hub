{
  "name": "practice-log-tests",
  "private": true,
  "type": "module",
  "description": "Test tooling for the practice log. Kept out of the repo root so Netlify never sees a package.json at the base directory and tries to build the site.",
  "scripts": {
    "test": "cd .. && ./scripts/check-all.sh"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
